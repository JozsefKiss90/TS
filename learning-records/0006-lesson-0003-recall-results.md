# Demonstrated: the SDK's absorbed mechanics vs the gateway's retained policy

Closed-book recall for lesson 0003 §7, collected 2026-07-27 (after the user completed lessons 0003 and 0004 and their labs). All four answers correct, mechanism-level, and in the user's own words.

**The [[sdk]] definitional gap (learning-records/0005) is now closed with evidence.** Under recall pressure the user produced the relational definition — "a language-specific library that turns an API's HTTP contract into convenient classes, methods, types, and errors for application code" — with the chore list (URLs/headers/bodies, auth + version headers, serialization/parsing incl. SSE, typed errors, retries) and the load-bearing negative: *it does not change the API's wire protocol*; plus two sophisticated caveats (SDK-identification headers; retries mean additional HTTP attempts). No trace of the 0005 stock-definition crutch. The watch-pattern from that record (reaching for dictionary definitions under pressure) did not reoccur anywhere in this session's eight answers.

**Evidence per question:**

- **Q2 (retry-after):** raw client could only *read* the header, "deciding whether to wait and resend was still my responsibility"; the SDK turns it into behavior for eligible failures, honoring server timing where available, backoff otherwise. Both knobs correct with code: constructor `maxRetries` as client-wide default, per-call second-argument override, `maxRetries: 0` disables — and the closer: "this is still retry *mechanics*: the SDK knows how to retry, but Hermes must decide which operations and contexts are *allowed* to retry." → promoted [[retry-with-backoff]], [[request-options]], [[api-client]].
- **Q3 (SDK `Message` vs handwritten interface):** "essentially the same limitation at runtime — neither TypeScript type validates incoming JSON after compilation"; `as` "tells TypeScript to trust my assertion"; the real difference is provenance/completeness/maintenance (reproduced the supplement's table in substance); "the SDK type deserves more design-time confidence… not because it possesses fundamentally stronger runtime trust"; names Zod as the tool if runtime trust is required. Confirms [[type-assertion]]/[[type-erasure]] (already demonstrated) and anticipates lesson 0005 exactly.
- **Q4 (what the gateway still owns):** model-routing/fallback policy (which model per task, whether fallback violates quality/privacy/evidence requirements) and budget/cancellation policy (SDK provides usage numbers, streaming, AbortSignal *mechanics*; Hermes decides the allowed budget, monitors, aborts). → promoted [[model-gateway]]. This is the input the 0006 ModelGateway lesson needs — the user already holds the mechanics/policy line it will build on.

**One sharpening given in chat:** "does not *generally* perform *complete* runtime schema validation" → the SDK performs **none** on success bodies (JSON syntax, then assertion; error bodies are classified by HTTP status into typed error classes, which is status-based behavior, not shape validation). **One addition:** timeouts are also retried (measured session 3: `APIConnectionTimeoutError` after 3 attempts) — the user's "certain transient server or connection errors" covered it only implicitly.

**Held at `introduced`:** [[typed-error]] (mentioned only definitionally; no class-level narrowing exercised), [[declaration-file]] (not exercised), [[runtime-validation]] (used correctly in passing — "must validate untrusted data explicitly, for example with Zod" — but its note sets the bar at *used in anger*; the 0005 lab Parts B/C are exactly that; promote there).
