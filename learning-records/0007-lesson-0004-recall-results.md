# Demonstrated: the streaming event grammar and cancellation as a cost lever

Closed-book recall for lesson 0004 §8, collected 2026-07-27, same session as the 0003 recall (learning-records/0006). All four answers correct; no misconceptions found.

**Evidence per question:**

- **Q1 ([[server-sent-events]]):** "a text-streaming format in which named `event:` lines and JSON-bearing `data:` lines are separated by blank lines, while the HTTP response remains open instead of completing after one response body" — format and the held-open connection, one sentence. → promoted.
- **Q2 (event grammar):** full order from memory — `message_start` skeleton (id, model, input-token usage) → `content_block_start` indexed slot → `content_block_delta` patches → `content_block_stop` → `message_delta` retrofits final `stop_reason` + true `output_tokens` → `message_stop`. The *why they can't arrive earlier* is epistemically exact: the server "cannot know why generation stopped until it actually stops — it might finish naturally, reach max_tokens, request a tool, or be interrupted", and cannot total tokens still being generated. → promoted [[delta]], [[stop-reason]]. (The `ping` went unmentioned — not asked; it never reaches user code anyway.)
- **Q3 (raw iterator vs helper):** raw `for await` over `create({ stream: true })` "exposes each typed `RawMessageStreamEvent`, so my code must narrow event types and perform the fold itself" — placed in Hermes's evidence/observability layer (event order, timestamps, intermediate state, policy decisions). Helper: `.on("text")`, `currentMessage`, **"retains the HTTP request_id"** (the lab's `_request_id: undefined` trap, correctly resolved), abort support, `finalMessage()`/`finalText()` — placed in the ordinary Phase 1 execution step that hands a completed `Message` to validation. Both placements sound. → promoted [[async-iterator]], [[message-stream]].
- **Q4 (⑥ transformed):** exercise 02's abort interrupted a *wait* (nothing kept); streaming abort interrupts *generation* — 41 characters kept, remaining 6 chunks "never generated and therefore not billed as output tokens". The budget argument is the lesson's, in stronger words: without mid-stream cancellation a budget "can check before starting and discover the final cost afterward, but cannot stop an in-flight generation" — advisory, not enforced. Bonus precision: since exact `output_tokens` arrive only at the end, the in-stream abort decision must use a conservative estimate — which is precisely S6's design problem in Phase 1. → promoted [[cancellation]].

**Carried forward:** the estimate-based-abort observation should be quoted back when Phase 1's bounds lesson (0009) designs budget enforcement — the user derived the requirement unprompted.

**Held at `introduced`:** [[discriminated-union]] and [[narrowing]] — Q3 brushes them ("narrow event types") but the mechanics get their third and cleanest workout in the 0005 lab's `safeParse` union; promote on that evidence.
